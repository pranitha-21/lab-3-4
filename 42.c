#include <stdio.h>
int main(){
	int N,i=2,a;
	printf("Enter N:");
	scanf("%d",&N);
	N=a;
	while(a>0)
	{
		N=N%i;
		break;
	}
	printf("%d",N);
	if(N%i==0)
		{
			printf("%d is a prime no.",a);
		}
return 0;
}
